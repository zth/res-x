// DOM-specific attribute helpers available on JSX elements when using ResX on the client.
// These are serialized string values produced by Client.Actions.make and Client.ValidityMessage.make.
export interface DomProps {
  "resx-onclick"?: string;
  "resx-validity-message"?: string;
}

