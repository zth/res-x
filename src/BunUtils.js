// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

let AssetPipeline$ResX = require("./AssetPipeline.js");

let isDev = process.env.NODE_ENV !== "production";

let projectRootPromise = {
  contents: undefined
};

function getProjectRoot() {
  let existingPromise = projectRootPromise.contents;
  if (existingPromise !== undefined) {
    return existingPromise;
  }
  let nextPromise = AssetPipeline$ResX.resolveProjectRoot(undefined);
  projectRootPromise.contents = nextPromise;
  return nextPromise;
}

async function serveStaticFile(request) {
  let projectRoot = await getProjectRoot();
  return await AssetPipeline$ResX.serveStaticFileFromBuild(request, projectRoot, isDev);
}

function runDevServer(port) {
  
}

function copy(search) {
  return new URLSearchParams(Object.fromEntries(search.entries()));
}

let URLSearchParams$1 = {
  copy: copy
};

exports.serveStaticFile = serveStaticFile;
exports.runDevServer = runDevServer;
exports.isDev = isDev;
exports.URLSearchParams = URLSearchParams$1;
/* isDev Not a pure module */
